services:
  petstore:
    image: swaggerapi/petstore3:latest
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:8080/api/v3/openapi.json | grep -q openapi || exit 1"]
      interval: 5s
      timeout: 10s
      retries: 12
